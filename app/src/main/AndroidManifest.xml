<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adminappcarrental">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdminAppCarRental">
        <activity android:name=".UserActivity"></activity>
        <activity android:name=".AddCarActivity" />
        <activity android:name=".AllTripActivity" />
        <activity android:name=".CarInforActivity" />
        <activity android:name=".AddNewCarActivity" />
        <activity
            android:name=".HomeAdminActivity"
            android:label="@string/title_activity_home_admin"
            android:theme="@style/Theme.AdminAppCarRental.NoActionBar" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Resignter" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>